# Google OAuth (Desktop app client) — console.cloud.google.com > APIs & Services > Credentials
# Create an OAuth client ID with Application type "Desktop app", then copy the values here into
# a real ".env" file (gitignored, never commit it).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
